initscript {
    dependencies {
        classpath files("%%CUSTOM_JAR%%") // If the plugin was published instead of copied we would just have to apply it
    }
}

allprojects {
    apply plugin: CustomModelInjectionPlugin
}